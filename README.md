# applications.who-am-i.service

## TODO

You’ll likely have something like:

MethodArgumentNotValidException → loop through FieldError

DataIntegrityViolationException → map to EMAIL_ALREADY_EXISTS

HttpMessageNotReadableException → map to MALFORMED_JSON
